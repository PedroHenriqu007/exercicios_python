# Peça base e altura e mostra a área do triângulo (área = base x altura / 2).

base = int(input("Digite a base do triângulo: "))
altura = int(input("Digite a altura do triângulo: "))

area = base * altura / 2

print(area)